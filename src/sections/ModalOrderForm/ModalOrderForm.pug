mixin ModalOrderForm(props)
	//- # ПОДКЛЮЧЕНИЕ ДЛЯ ПРОВЕРОК DATA, OPT, ELEM # -//
	include ../../shared/helpers/pug/shortcut-check-store

	//- # ПОЛУЧЕНИЕ CSS КЛАССОВ КОМПОНЕНТА # -//
	include ModalOrderForm.cn.pug
	- const styles = classes(props && props.cn);

	div(class=styles.modalOrderForm data-dialog-name=DATA('dialogName', false))
		+I('Modal', ELEM('Modal'))
			+ModalBody()
				+I('Form', ELEM('Form'))(class=styles.modalOrderFormForm)
