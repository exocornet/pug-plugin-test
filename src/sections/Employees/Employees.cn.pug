-
	function classes(cn) {
		const BLOCK = CN('employees');

		return {
			employees: BLOCK({}, [cn?.margin]),
			employeesRow: BLOCK('row', {
				[`${cn?.rowSlideMobile}`]: ['slide'].includes(cn?.rowSlideMobile),
			}),
			employeesCol: BLOCK('col', [cn?.jsClassResize]),
		}
	}
