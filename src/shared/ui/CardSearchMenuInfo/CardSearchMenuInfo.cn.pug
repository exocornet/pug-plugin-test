-
	function classes(cn) {
		const BLOCK = CN('card-search-menu-info')

		return {
			cardSearchMenuInfo: BLOCK({}, ['d-flex', 'flex-direction-column', 'justify-content-between']),
			cardSearchMenuInfoBackground: BLOCK('background'),
		}
	}
