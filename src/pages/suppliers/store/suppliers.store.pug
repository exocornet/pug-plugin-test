//- # ПАРАМЕТРЫ ДЛЯ НАСТРОЙКИ LAYOUT # -//
-
	const pageTitle = 'Поставщикам';
	// const isSvgSprite = true;
	// const classBodyPage = 'about-body';
	// const classMainPage = 'about-main';

	const Header = {
		elem: {
			BreadCrumbs: {
				dataArr: {
					LinksArr: [
						{ data: { text: 'Главная' } },
						{ data: { text: 'Поставщикам' } },
					]
				}
			}
		}
	};

//- # НАСЛЕДОВАНИЕ ФУНКЦИИ ДЛЯ РАЗДЕЛЕНИЯ СТОРОВ # -//
- const pageStore = addSections();

//- # Подключение store для секции HeadingDetailed # -//
include suppliers--heading-detailed
- pageStore.addSection(HeadingDetailed);

//- # Подключение store для секции ImageFacts # -//
include suppliers--image-facts
- pageStore.addSection(ImageFacts);

//- # Подключение store для секции PageLinks # -//
include suppliers--page-links-services
- pageStore.addSection(PageLinksServices);

//- # Добавление store для секции SliderInfoCard # -//
include suppliers--slider-info-card
- pageStore.addSection(SliderInfoCard);

//- # Подключение store для секции PageLinks # -//
include suppliers--page-links-purchase-policy
- pageStore.addSection(PageLinksPurchasePolicy);

//- ## Подключение store секции ListContactLinks ## -//
include suppliers--list-contact-links
- pageStore.addSection(ListContactLinks);

//- ## Подключение store секции SubscriptionForm ## -//
include suppliers--subscription-form
- pageStore.addSection(SubscriptionForm);

//- ## Подключение store секции FeedbackForm ## -//
include suppliers--feedback-form
- pageStore.addSection(FeedbackForm);

//- ## Подключение store секции HiddenDialogContent ## -//
include suppliers--dialog
- pageStore.addSection(HiddenDialogContent);
