//- # ПАРАМЕТРЫ ДЛЯ НАСТРОЙКИ LAYOUT # -//
-
	const pageTitle = 'Наша история';
	// const isSvgSprite = true;
	// const classBodyPage = 'about-body';
	// const classMainPage = 'about-main';

	const Header = {
		elem: {
			BreadCrumbs: {
				dataArr: {
					LinksArr: [
						{ data: { text: 'Главная' } },
						{ data: { text: 'О компании' } },
						{ data: { text: 'Наша история' } }
					]
				}
			}
		}
	};

//- # НАСЛЕДОВАНИЕ ФУНКЦИИ ДЛЯ РАЗДЕЛЕНИЯ СТОРОВ # -//
- const pageStore = addSections();

//- ## Подключение store секции SliderHistory ## -//
include history--slider-history
- pageStore.addSection(SliderHistory);

//- ## Подключение store секции Excursion ## -//
include history--excursion
- pageStore.addSection(Excursion);

//- ## Подключение store секции о Career## -//
include history--career
- pageStore.addSection(Career);

//- ## Подключение store секции PageLinks ## -//
include history--page-links
- pageStore.addSection(PageLinks);

//- ## Подключение store секции HiddenDialogContent ## -//
include history--dialog
- pageStore.addSection(HiddenDialogContent)

//- ## Подключение store секции ModalForm ## -//
include history--modal-form
- pageStore.addSection(ModalForm)
