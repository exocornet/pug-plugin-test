//- # ПАРАМЕТРЫ ДЛЯ НАСТРОЙКИ LAYOUT # -//
-
	const pageTitle = 'Контакты';

	const Header = {
		elem: {
			BreadCrumbs: {
				dataArr: {
					LinksArr: [
						{data: {text: 'Главная'}},
					]
				}
			}
		}
	};

	const Footer = {
		opt: {
			notContacts: true
		},
		cn: {
			margin: 'mt-120 mt-80@xs',
			padding: 'pb-0'
		}
	};

//- # НАСЛЕДОВАНИЕ ФУНКЦИИ ДЛЯ РАЗДЕЛЕНИЯ СТОРОВ # -//
- const pageStore = addSections();

//- ## Подключение store секции HeadingMap ## -//
include contacts--heading-map
- pageStore.addSection(HeadingMap);

//- ## Подключение store секции ListContactLinks ## -//
include contacts--list-contact-links
- pageStore.addSection(ListContactLinks);

//- ## Подключение store секции Requisites ## -//
include contacts--requisites
- pageStore.addSection(Requisites);

//- ## Подключение store секции ListInfoCard ## -//
include contacts--list-info-card
- pageStore.addSection(ListInfoCard);

//- ## Подключение store секции FeedbackForm ## -//
include contacts--feedback-form
- pageStore.addSection(FeedbackForm);

//- ## Подключение store секции HiddenDialogContent ## -//
include contacts--dialog
- pageStore.addSection(HiddenDialogContent);
