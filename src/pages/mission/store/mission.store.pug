//- # ПАРАМЕТРЫ ДЛЯ НАСТРОЙКИ LAYOUT # -//
-
	const pageTitle = 'Миссия и ценности';
	// const isSvgSprite = true;
	// const classBodyPage = 'about-body';
	// const classMainPage = 'about-main';

	const Header = {
		elem: {
			BreadCrumbs: {
				dataArr: {
					LinksArr: [
						{data: {text: 'Главная'}},
						{data: {text: 'о компании'}},
						{data: {text: 'Миссия и ценности'}},
					]
				}
			}
		}
	};

//- # НАСЛЕДОВАНИЕ ФУНКЦИИ ДЛЯ РАЗДЕЛЕНИЯ СТОРОВ # -//
- const pageStore = addSections();

//- ## Подключение store секции HeadingDetailed ## -//
include mission--heading-detailed
- pageStore.addSection(HeadingDetailed);

//- ## Подключение store секции ImageFacts ## -//
include mission--image-facts
- pageStore.addSection(ImageFacts);

//- ## Подключение store секции ImageHeading Development ## -//
include mission--steps-animated-circle
- pageStore.addSection(StepsAnimatedCircle);

//- ## Подключение store секции Career## -//
include mission--career-mission
- pageStore.addSection(CareerMission);

//- ## Подключение store секции Excursion ## -//
include mission--excursion
- pageStore.addSection(Excursion);

//- ## Подключение store секции о Career ## -//
include mission--career-kemerovo
- pageStore.addSection(CareerKemerovo);

//- ## Подключение store секции PageLinks ## -//
include mission--page-links
- pageStore.addSection(PageLinks);

include mission--dialog
- pageStore.addSection(HiddenDialogContent)

include mission--modal-form
- pageStore.addSection(ModalForm)
