//- # ПАРАМЕТРЫ ДЛЯ НАСТРОЙКИ LAYOUT # -//
-
	const pageTitle = 'Управление компанией';
	// const isSvgSprite = true;
	// const classBodyPage = 'management-body';
	// const classMainPage = 'management-main';

	const Header = {
		elem: {
			BreadCrumbs: {
				dataArr: {
					LinksArr: [
						{ data: { text: 'Главная' } },
						{ data: { text: 'О компании' } },
						{ data: { text: 'Управление компанией' } }
					]
				}
			}
		}
	};

//- # НАСЛЕДОВАНИЕ ФУНКЦИИ ДЛЯ РАЗДЕЛЕНИЯ СТОРОВ # -//
- const pageStore = addSections();

//- ## Подключение store секции HeadingDetailed ## -//
include management--heading-detailed
- pageStore.addSection(HeadingDetailed);

//- ## Подключение store секции ImageFacts ## -//
include management--image-facts
- pageStore.addSection(ImageFacts);

//- ## Подключение store секции TabsTeam ## -//
include management--tabs-team
- pageStore.addSection(TabsTeam);

//- ## Подключение store секции SliderCompany ## -//
include management--slider-company
- pageStore.addSection(SliderCompany);

//- ## Подключение store секции PageLinks ## -//
include management--page-links
- pageStore.addSection(PageLinks);
