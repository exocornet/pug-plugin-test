//- # ПАРАМЕТРЫ ДЛЯ НАСТРОЙКИ LAYOUT # -//
-
	const pageTitle = 'Цели и стратегия компании';
	// const isSvgSprite = true;
	// const classBodyPage = 'about-body';
	// const classMainPage = 'about-main';

	const Header = {
		elem: {
			BreadCrumbs: {
				dataArr: {
					LinksArr: [
						{ data: { text: 'Главная' } },
						{ data: { text: 'О компании' } },
						{ data: { text: 'Цели и стратегия' } }
					]
				}
			}
		}
	};

//- # НАСЛЕДОВАНИЕ ФУНКЦИИ ДЛЯ РАЗДЕЛЕНИЯ СТОРОВ # -//
- const pageStore = addSections();

//- # Добавление store секции HeadingDetailed # -//
include goals-and-strategies--heading-detailed
- pageStore.addSection(HeadingDetailed);

//- # Добавление store секции ImageFacts # -//
include goals-and-strategies--image-facts
- pageStore.addSection(ImageFacts);

//- # Добавление store секции TitleTextWrap # -//
include goals-and-strategies--title-text-wrap
- pageStore.addSection(TitleTextWrap);

//- # Добавление store секции TitleTextWrapGoals # -//
include goals-and-strategies--title-text-wrap-goals
- pageStore.addSection(TitleTextWrapGoals);

//- # Добавление store секции ArrowedSection # -//
include goals-and-strategies--arrowed-section
- pageStore.addSection(ArrowedSection);

//- ## Подключение store секции ListInfoCard ## -//
include goals-and-strategies--list-info-card
- pageStore.addSection(ListInfoCard);

//- # Добавление store секции PageLinks # -//
include goals-and-strategies--page-links
- pageStore.addSection(PageLinks);
