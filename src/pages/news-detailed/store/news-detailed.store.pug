//- # ПАРАМЕТРЫ ДЛЯ НАСТРОЙКИ LAYOUT # -//
-
	const pageTitle = 'Новости. Детальная страница';
	// const isSvgSprite = true;
	// const classBodyPage = 'about-body';
	// const classMainPage = 'about-main';

	const Header = {
		elem: {
			BreadCrumbs: {
				dataArr: {
					LinksArr: [
						{ data: { text: 'Главная' } },
						{ data: { text: 'Пресс-центр' } },
						{ data: { text: 'Новости' } },
						{ data: { text: 'События компании' } },
					]
				}
			}
		}
	};

//- # НАСЛЕДОВАНИЕ ФУНКЦИИ ДЛЯ РАЗДЕЛЕНИЯ СТОРОВ # -//
- const pageStore = addSections();

//- # Подключение store секции HeadingDetailed # -//
include news-detailed--heading-detailed
- pageStore.addSection(HeadingDetailed);

//- # Подключение store секции ImageFacts # -//
include news-detailed--image-facts
- pageStore.addSection(ImageFacts);

//- # Подключение store секции TitleTextWrap # -//
include news-detailed--title-text-wrap
- pageStore.addSection(TitleTextWrap);

//- # Подключение store секции FactsWrap # -//
include news-detailed--facts-wrap
- pageStore.addSection(FactsWrap);

//- # Подключение store секции CardQuoteWrap # -//
include news-detailed--card-quote-wrap
- pageStore.addSection(CardQuoteWrap);
media
//- # Подключение store секции TitleTextWrap # -//
include news-detailed--title-text-wrap-quote
- pageStore.addSection(TitleTextWrapQuote);

//- ## Подключение store секции SubscriptionForm ## -//
include news-detailed--subscription-form
- pageStore.addSection(SubscriptionForm);

//- # Подключение store секции PageLinks # -//
include news-detailed--page-links
- pageStore.addSection(PageLinks);

//- ## Подключение store секции ListContactLinks ## -//
include news-detailed--list-contact-links
- pageStore.addSection(ListContactLinks);

//- ## Подключение store секции HiddenDialogContent ## -//
include news-detailed--dialog
- pageStore.addSection(HiddenDialogContent);
