//- # ПАРАМЕТРЫ ДЛЯ НАСТРОЙКИ LAYOUT # -//
-
	const pageTitle = 'Программы компании. Список';
	// const isSvgSprite = true;
	// const classBodyPage = 'about-body';
	// const classMainPage = 'about-main';

	const Header = {
		elem: {
			BreadCrumbs: {
				dataArr: {
					LinksArr: [
						{ data: { text: 'Главная' } },
						{ data: { text: 'Карьера' } },
					]
				}
			}
		}
	};

//- # НАСЛЕДОВАНИЕ ФУНКЦИИ ДЛЯ РАЗДЕЛЕНИЯ СТОРОВ # -//
- const pageStore = addSections();

//- ## Подключение store секции HeadingDetailed ## -//
include company-programs-list--heading-detailed
- pageStore.addSection(HeadingDetailed);

//- ## Подключение store секции ImageFacts ## -//
include company-programs-list--image-facts
- pageStore.addSection(ImageFacts);

//- ## Подключение store секции SliderInfoCard ## -//
include company-programs-list--slider-info-card-programs
- pageStore.addSection(SliderInfoCardPrograms);

//- ## Подключение store секции Career ## -//
include company-programs-list--career
- pageStore.addSection(Career);

//- ## Подключение store секции VideoFacts ## -//
include company-programs-list--video-facts
- pageStore.addSection(VideoFacts);

//- ## Подключение store секции HeadingDetailed ## -//
include company-programs-list--heading-detailed-video.pug
- pageStore.addSection(HeadingDetailedVideo);

//- ## Подключение store секции ArrowedSection ## -//
include company-programs-list--arrowed-section
- pageStore.addSection(ArrowedSection);

//- ## Подключение store секции PageLinks ## -//
include company-programs-list--page-links
- pageStore.addSection(PageLinks);

//- ## Подключение store секции FeedbackForm ## -//
include company-programs-list--feedback-form
- pageStore.addSection(FeedbackForm);

include company-programs-list--dialog
- pageStore.addSection(HiddenDialogContent);
