//- # ПАРАМЕТРЫ ДЛЯ НАСТРОЙКИ LAYOUT # -//
-
	const pageTitle = 'О группе компаний';
	// const isSvgSprite = true;
	// const classBodyPage = 'about-body';
	// const classMainPage = 'about-main';

	const Header = {
		elem: {
			BreadCrumbs: {
				dataArr: {
					LinksArr: [
						{ data: { text: 'Главная' } },
						{ data: { text: 'О компании' } },
						{ data: { text: 'О группе компаний' } },
					]
				}
			}
		}
	};

//- # НАСЛЕДОВАНИЕ ФУНКЦИИ ДЛЯ РАЗДЕЛЕНИЯ СТОРОВ # -//
- const pageStore = addSections();

//- # Подключение store секции PageLinks # -//
include about-group-companies--facts-align-right
- pageStore.addSection(FactsAlignRight);

//- ## Подключение store секции TabsMap ## -//
include about-group-companies--tabs-map
- pageStore.addSection(TabsMap);

//- # Подключение store секции PageLinks # -//
include about-group-companies--page-links
- pageStore.addSection(PageLinks);
